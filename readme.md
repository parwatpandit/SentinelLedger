activate the virtual invaronment--> source venv/bin/activate
pip freeze > requirements.txt => for auto update the .txt fiel when install a library using pip so i know what are the library i have
mysqlworkbench password: Maninblack90
run main.py => uvicorn main:app --reload

pusihing in git:   git init
                   git add .
                   git commit -m "SentinelLedger v2 backend complete"